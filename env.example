# OpenAI API Key (required for all AI features)
OPENAI_API_KEY=sk-your-key-here

# Database path (for example app)
DATABASE_PATH=./data/workshop.db

# JWT Secret (for example app authentication)
JWT_SECRET=workshop-secret-change-in-production

# Background processing (for example app)
CRON_SECRET=internal-cron-secret
API_URL=http://localhost:3000
